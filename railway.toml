[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && npm run start:with-migration"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Railway will watch the entire repo, but we set rootDirectory in Railway dashboard to "backend"
