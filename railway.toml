[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start:with-migration"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Note: Set Root Directory to "backend" in Railway Dashboard
# If Root Directory is not set, use: startCommand = "cd backend && npm run start:with-migration"
