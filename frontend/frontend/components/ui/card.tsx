import * as React from "react";`nimport { motion } from "framer-motion";`nimport { cn } from "./utils";`n`ninterface CardProps extends React.ComponentProps<"div"> {  hover?: boolean;  onClick?: () => void;}`n`nfunction Card({ className, hover = false, onClick, children, ...props }: CardProps) {  const baseStyles = cn(    "bg-card text-card-foreground flex flex-col gap-6 rounded-xl border",    className,`n  );  const hoverStyles = hover || onClick ? "cursor-pointer transition-shadow hover:shadow-lg" : "";  const content = (    <div className={cn(baseStyles, hoverStyles)} {...props}>      {children}    </div>`n  );  if (onClick || hover) {  `n  return (`n      <motion.div        whileHover={hover || onClick ? { scale: 1.01 } : undefined}        whileTap={onClick ? { scale: 0.99 } : undefined}        onClick={onClick}      >        {content}      </motion.div>  `n  );  }  return content;}`n`nfunction CardHeader({ className, ...props }: React.ComponentProps<"div">) {`n  return (`n    <div      data-slot="card-header"      className={cn(        "@container/card-header grid auto-rows-min grid-rows-[auto_auto] items-start gap-1.5 px-6 pt-6 has-data-[slot=card-action]:grid-cols-[1fr_auto] [.border-b]:pb-6",        className,      )}      {...props}    />`n  );}`n`nfunction CardTitle({ className, ...props }: React.ComponentProps<"div">) {`n  return (`n    <h4      data-slot="card-title"      className={cn("leading-none", className)}      {...props}    />`n  );}`n`nfunction CardDescription({ className, ...props }: React.ComponentProps<"div">) {`n  return (`n    <p      data-slot="card-description"      className={cn("text-muted-foreground", className)}      {...props}    />`n  );}`n`nfunction CardAction({ className, ...props }: React.ComponentProps<"div">) {`n  return (`n    <div      data-slot="card-action"      className={cn(        "col-start-2 row-span-2 row-start-1 self-start justify-self-end",        className,      )}      {...props}    />`n  );}`n`nfunction CardContent({ className, ...props }: React.ComponentProps<"div">) {`n  return (`n    <div      data-slot="card-content"      className={cn("px-6 [&:last-child]:pb-6", className)}      {...props}    />`n  );}`n`nfunction CardFooter({ className, ...props }: React.ComponentProps<"div">) {`n  return (`n    <div      data-slot="card-footer"      className={cn("flex items-center px-6 pb-6 [.border-t]:pt-6", className)}      {...props}    />`n  );}`n`nexport {  Card,  CardHeader,  CardFooter,  CardTitle,  CardAction,  CardDescription,  CardContent,};

